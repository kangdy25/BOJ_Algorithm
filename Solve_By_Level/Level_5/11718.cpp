// 그대로 출력하기 / 브론즈 3

#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(0); 
    cin.tie(0); 
    string str;
    
    while(1) {
        getline(cin, str);
        if (str == "") break;
        cout << str << "\n";
    }
}