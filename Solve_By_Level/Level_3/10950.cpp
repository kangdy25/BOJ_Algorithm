// A + B - 3 / 브론즈 5

#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int Case, a, b;
    cin >> Case;
    
    for (int i = 0; i < Case; i++) {
        cin >> a >> b;
        cout << a + b << "\n";
    }
}